# Principais alterações (sessão de automação)

## Convenção de manutenção

Sempre que um ajuste neste projeto for **concluído** (código, Cypress, env, scripts, documentação de testes, etc.), este arquivo deve ser **atualizado na mesma entrega**: acrescentar uma linha ou bloco em **Registro cronológico** (data + o que mudou + arquivos relevantes). Objetivo: histórico legível para equipe e para revisão sem depender só do Git.

A regra também está descrita em `.cursor/rules/atualizar-alteracoes-md.mdc` para sessões futuras no Cursor.

---

Data de referência inicial: 2026-05-04.

## Contexto

A suíte E2E (Cypress) falhava em vários fluxos de **compras** com o erro *“Não foi possível localizar o emissor na lista”*. O emissor usado (`ACOS FAVORIT DISTRIBUIDORA LTDA.`) não aparecia mais na lista do ambiente **QA**, enquanto login, pedidos e outros fluxos seguiam funcionando com outro cadastro.

## O que foi alterado (visão geral)

### Configuração de emissor

| Arquivo | Alteração |
|---------|-----------|
| `cypress.env.json` | Valor de `emissor` alinhado ao QA (ex.: `ACOCON` / cadastro disponível); não versionar segredos. |
| `cypress.env.example.json` | Valores de exemplo para novos clones. |

### Specs E2E

Emissor centralizado via `Cypress.env('emissor')` e `ComprarPage.selecionaEmissorCorretamente()` onde aplicável. Evolução da suíte: compras, menu, finanças, documentos, pedidos, fluxoCompra, Allure, workflow GitHub Pages, relatório gerencial HTML/PDF, Gherkin, etc.

## Execução de testes

- **`npm run cy:run`** na pasta `gerdau_mais_automation` (ou atalhos na raiz do monorepo).
- **PowerShell (Windows):** preferir `Set-Location "caminho"; npm run ...` se `&&` não for suportado.

## Documentação BDD (`.feature`)

Arquivos em `docs/bdd/features/` são referência; a implementação executável está em `cypress/e2e/**/*.cy.js`.

## Próximos passos sugeridos (opcional)

- Ajustar `emissor` / credenciais no `cypress.env.json` ou `CYPRESS_*` no CI conforme o QA.
- Painel Allure: `docs/ALLURE.md` (local e GitHub Pages).

---

## Registro cronológico

| Data | Resumo |
|------|--------|
| 2026-05-04 | Criado `docs/ALTERACOES.md` com histórico do emissor QA, specs usando `Cypress.env('emissor')`, notas PowerShell / `cy:open` / BDD. |
| 2026-05-04 | Incluída **Convenção de manutenção**. Criada regra `.cursor/rules/atualizar-alteracoes-md.mdc` (`alwaysApply: true`). |
| 2026-05-05 | Carrinho (AUT-009/010/012), menu superior, finanças, documentos, scripts npm; robustez emissor em `comprarPageEmissorActions.js`. |
| 2026-05-06 a 2026-05-11 | Iterações em helpers/pages/compras (checkout, modais, planilha, histórico, etc.); conferir commits locais para detalhe. |
| 2026-05-12 | Auditoria E2E, specs auth/pedidos/planilha/vitrine, `GUIA-COBERTURA`, relatório textual, checklist/HTML gerencial, PDF, matriz. |
| 2026-05-13 | `cy:run:headed`; relatório 5.1 mobile/Chrome; PDF; **Allure** (`allure-commandline`, `scripts/allure-cli.js`, scripts npm, `docs/ALLURE.md`). |
| 2026-05-13 | **GitHub Pages:** `.github/workflows/allure-pages.yml`, push + `workflow_dispatch`, segredos `CYPRESS_*`. |
| 2026-05-13 | **ALLURE.md:** Opções A/B deploy em detalhe. **Gherkin** alinhado aos specs (`docs/bdd`). |
| 2026-05-13 | Removida seção “roteiro de vídeo” de `docs/ALLURE.md`. **`docs/ALTERACOES.md` restaurado** após corrupção acidental (histórico condensado; detalhes antigos nos commits). |
| 2026-05-13 | Adicionado **`LICENSE`** (ISC) na raiz do repositório, alinhado ao campo `"license": "ISC"` em `gerdau_mais_automation/package.json`. |
| 2026-05-13 | **GitHub:** `docs/GITHUB.md` (Git no Windows, estrutura do monorepo, `git push` com PAT, segredos `CYPRESS_*`, Pages). `README.md` na raiz do workspace; link no `gerdau_mais_automation/README.md` e em `docs/ALLURE.md`. **Git:** `git init` na raiz, branch `main`, commit inicial (remoto ainda não configurado — ver `docs/GITHUB.md`). |
| 2026-05-13 | **Performance E2E (datas/checkout):** em `fluxoCompra.js`, pausas de calendário centralizadas (`MS_CAL_*`), `cy.wait` 400–700 ms → ~180–280 ms, `type` da data com `delay` menor, overlay inicial do carrinho 10 s → 7 s, detecção de modal `4 s` → `3,2 s`. Mantém reforço condicional quando “Selecione uma data.” persiste. |
| 2026-05-18 | Checkout E2E: `acessarCatalogoVitrineComEmissor` em `fluxoCompra.js` (substitui `cy.visit` no catálogo); **Finalizar** / **Avançar revisão** com rótulos alternativos, scroll e CTA habilitado; datas com scroll prévio. Specs `buscaSemResultados.cy.js` e `compraPorHistorico.cy.js` atualizados. |
| 2026-05-18 | **`fluxoCompra.js`:** `textoAcumuladoElemento` passa a considerar o atributo **`value`** (CTA `input[type=submit|button]` sem `textContent`), corrigindo timeout em `clicarFinalizarPedido` na suíte de compras (6/7 specs falhavam no QA). |
| 2026-05-18 | **`preencherDatasDesejadasNoCarrinho`:** `aguardarFimCarregamentoTextual` após overlay do carrinho; leve aumento em `MS_CAL_*`; checagem inicial de reforço com timeout 8 s — reduz falha intermitente em `carrinhoPersistenciaRelogin.cy.js` (“Selecione uma data.”). |
| 2026-05-18 | **`docs/ALLURE.md`:** URL do painel [davibrito-ilab.github.io/gerdaumais](https://davibrito-ilab.github.io/gerdaumais/#) e passos para **atualizar** o GitHub Pages (workflow manual vs push). |
| 2026-05-18 | **Documentação de cobertura:** `relatorio-gerencial-automacao-e2e.html`, `CHECKLIST-PRE-DEPLOY-COBERTURA-AUTOMACAO-E2E.md`, `GUIA-COBERTURA-AUTOMACAO-E2E.md`, `matriz-cenarios-automacao.md`, `ARQUITETURA.md` — referência **2026-05-18** (suite 31/33 até novos specs de pedidos). |
| 2026-05-18 | **Pedidos corte/dobra:** 2º cenário em `pedidosCorteEDobra.cy.js` (período opcional + busca); helper `buscarPedidosCarteiraFlex` para CTAs que não usam o texto “Buscar pedidos”; `validarListaResponde` aceita termos corte/dobra/aço. Docs + BDD + GUIA/Checklist (**36** `it`). |
| 2026-05-18 | **Pedidos E2E:** `preencherPeriodoUltimosDiasSeDisponivel` + `input[type="button"]` em `pedidosFiltros.js`; specs `pedidosPeriodoBusca.cy.js`, `pedidosHubRecarregar.cy.js`; script `npm run cy:run:pedidos`; docs + BDD `04-pedidos.feature`. |
| 2026-05-18 | **Compra Corte e dobra (fabricação):** rotas QA `/purchase/fabrication/*`; `fluxoCompra.js` (`acessarIdentificacaoObraFabricacaoComEmissor`, `acessarHistoricoPedidosFabricacaoComEmissor`, constantes/regEx), `comprarPageTipoCompraActions.js` / `comprarPageMetods.js`; spec `compraCorteEDobra.cy.js` (2× `it`); `npm run cy:run:compra-corte-dobra`; BDD `03-compras-p1.feature` (AUT-039/040); GUIA/CHECKLIST/matriz/`RELATORIO` — **31** specs / **38** testes. |
| 2026-05-18 | **Histórico Comprar → Revisar pedido:** `fluxoCompra.js` (`REGEX_INDICIOS_ETAPA_REVISAR_PEDIDO`, `avancarCarrinhoAteEtapaRevisarPedido`, `finalizarPedidoNoCarrinho` passa por esse helper); `historicoRepeatCompra.js`; `pedido.js` usa o mesmo regex de revisão; spec `compraHistoricoRevisarPedido.cy.js`; script `npm run cy:run:compra-historico-revisar`; BDD `03-compras-p1.feature` (AUT-041); GUIA/CHECKLIST/matriz/`RELATORIO` — totais **32** specs / **40** testes. |
| 2026-05-18 | **Compra adicional — histórico sem vitrine + vitrine dois itens:** specs `compraHistoricoFinalizePedido.cy.js` (AUT-042 — envio apenas via histórico repetível) e `compraVitrineDoisItens.cy.js` (AUT-043 — `produto` + `produto2` opcional); scripts `cy:run:compra-historico-finalize` e `cy:run:compra-vitrine-dois-itens` em `package.json`; atualização GUIA/CHECKLIST/`RELATORIO`/matriz/HTML gerencial/`03-compras-p1.feature` — totais documentados **34** specs / **42** casos `it`. |
| 2026-05-18 | **Complementares Compras (checklist §2–§4):** `planilhaUploadComplementares.cy.js` (4× `it` — SKU/colunas/XLSX 1 linha até carrinho/2 linhas na grade); `compraVitrineTresItens.cy.js`; `carrinhoNavegacaoVoltar.cy.js`; `fluxoCompra.js` (`navegarTelaSpreadsheetComEmissor`, `anexarArquivoFluxoSpreadsheet`); **`xlsx`** (devDependency) + `scripts/generate-planilha-fixtures.js` + `npm run fixtures:planilhas` → `cypress/fixtures/planilha-qa-*.xlsx`; refactor `planilhaUploadArquivoInvalido.cy.js`; 3º `it` opcional revisão cart em `compraCorteEDobra.cy.js`; scripts `cy:run:planilha-complementares`, `cy:run:compra-vitrine-tres-itens`, `cy:run:carrinho-voltar`; AUT-044–050 em `docs/bdd/features/03-compras-p1.feature`; atualização GUIA/CHECKLIST/`RELATORIO`/matriz/HTML — **37** specs / **48** `it`. |
| 2026-05-18 | **Vitrine — detalhe + inclusão opcional ao carrinho:** segundo `it` em `vitrineMaisDetalhes.cy.js` (shared `prepararCatalogoVitrine`, `ADD_TO_CART_SELECTORS`, `assertTextoConfirmacaoCarrinho`; `skip` sem CTA de detalhes ou sem inclusão típica na vista); AUT-051 em `docs/bdd/features/03-compras-p1.feature` e `docs/bdd/README.md`; contagens revisadas (**49** `it`) — CHECKLIST §2.2, GUIA, `RELATORIO-AUTOMACAO-E2E-TEXTO-COMPLETO`, matriz, `relatorio-gerencial-automacao-e2e.html`. |
| 2026-05-18 | **Operação QA:** script `npm run cy:run:vitrine-mais-detalhes` em `package.json`; documentado em `README.md`, GUIA §6 scripts, CHECKLIST §2.1, `RELATORIO-AUTOMACAO-E2E-TEXTO-COMPLETO` §6; `bdd/README.md` atualizado quando o inventário de specs mudar. |
| 2026-05-18 | **AUT-015 vitrine:** spec `vitrineFiltroCatalogoFamilia.cy.js`; script `npm run cy:run:vitrine-filtro-catalogo`; `comprarPageHelpers.js` + `comprarPage.js` (getters **add-to-cart** / **mais detalhes**); vitrine Mais detalhes importa **`REGEX_CTA_DETALHES_LISTAGEM`**; inventário **38** specs / **50** `it` — BDD/README/CHECKLIST/GUIA/matriz/HTML/`RELATORIO`. |
| 2026-05-19 | Performance regressão local/CI: `npm run cy:run:fast` e `npm run cy:run:headed:fast` (`video=false`), `package.json`; notas em `README.md`, `GUIA-COBERTURA-AUTOMACAO-E2E.md` §6, `RELATORIO-AUTOMACAO-E2E-TEXTO-COMPLETO.md` §6. |
| 2026-05-19 | **Roadmap (checkout/P1):** `fluxoCompra.js` — CTA final com mais regex, rodapés, `PRIORIDADE_CTA_FINALIZAR_SELECTOR` (`data-testid`/`data-cy`); refinamento datas com scroll topo + `reforcoEventosData`; documentação inline do contrato. **Busca sem resultados / sem emissor:** specs desacoplados smoke vs `@p2` até envio. **Planilha complementares:** timeouts em `STEP_TIMEOUT` nos asserts decisivos. **AUT-017/018:** `pedidosDetalhePedido.cy.js` + `pedidosFiltros.js` (`clicarPrimeiroPossivelLinkDetalhesPedido`); segundo `it` em `documentosBusca.cy.js`. **Navegação cross (AUT-052):** `navegacaoCruzadaComprasPedidos.cy.js`; `npm run cy:run:menu`/`cy:run:operacional-modulos`/`cy:run:regression-nightly` agora cobrem `cypress/e2e/menu/*.cy.js`; `npm run cy:run:navegacao-cross`; BDD `04-pedidos.feature`, `05-menu-financas-documentos.feature`, `docs/bdd/README.md`. Inventário aprox.: **40** specs / **55** casos `it` (conferir § **Resumo numérico** do `RELATORIO-AUTOMACAO-E2E-TEXTO-COMPLETO.md`). |
| 2026-05-19 | **AUT-017 condicional:** `pularSeGradePedidosVaziaAposBusca` em `pedidosFiltros.js` + uso em `pedidosDetalhePedido.cy.js` (SKIP se grade vazia após buscar). Sincronização inventário **40 / 55** nos docs: `GUIA-COBERTURA-AUTOMACAO-E2E.md` (lacunas AUT-017/018), `CHECKLIST-PRE-DEPLOY-COBERTURA-AUTOMACAO-E2E.md`, `matriz-cenarios-automacao.md`, `relatorio-gerencial-automacao-e2e.html`. |
| 2026-05-19 | **Validação `npm run cy:run:fast` (ambiente QA):** rodada única (~44m37s no resumo Cypress, ~48m total terminal); **32** passing / **19** failing / **4** pending em **55** `it`; **16/40** specs com pelo menos uma falha; **`exit_code` 19**. Padrões principais nos logs: **CTA final** (`fluxoCompra.js` — `clicarFinalizarPedido`/`textoAcumuladoElemento`), timeouts **overlay/modal** (ex.: `planilhaUploadComplementares.cy.js`), **URL sem `/shopping-cart`**. **Pending:** `pedidosDetalhePedido` (1 — grade vazia), `vitrineFiltroCatalogoFamilia` (1), `vitrineMaisDetalhes` (2) — conferir tabela “Run Finished”. Avisos locais (Windows): falha ao “trash” de `cypress/screenshots`; políticas de Chrome citadas pelo Cypress — não alteram resultado isoladamente dos asserts. |
| 2026-05-19 | **Estabilização pós-validação QA:** `comprarPageHelpers.js` — `OVERLAY_SELECTORS` menos genéricos (sem `.modal`/`.loading` soltos duplicados). **`fluxoCompra.js`** — `bodyOuPathnameIndicaCarrinhoOuCheckout` + `esperarFluxoCarregamentoCarrinho`; `irParaCarrinhoViaHeader`/`avancarParaOCarrinho` aceitam **SPA**/drawer sem `/shopping-cart`; `clicarFinalizarPedido` espera overlay/loading, amplia rodapés/sticky/`data-testid` **ConfirmOrder**, sinonímios **`casaComTextoFinalizarPedido`**, fallback **`cy.contains`**. |
| 2026-05-19 | **Segunda rodada `npm run cy:run:fast` (QA):** **`exit_code` 20**; **55** `it`: **31** passing / **20** failing / **4** pending (~54 min no resumo); **17/40** specs com falha (≈43%). Problemas recorrentes nos logs: **checkout / CTA final**, **overlay/aguardar tela**, **entrada na cesta**. |
| 2026-05-19 | **Patches após a 2ª rodada:** `comprarPageHelpers.js` — tirar **`[aria-modal="true"]`** da lista global; ignorar overlays com **`aria-hidden`/`hidden`**. **`fluxoCompra.js`** — URLs **commerce**/**order-items** e mais texto de **cesta**; fallback do CTA com **`span.hefesto-button__label`** + **`.closest()`** ao pai clicável; `PRIORIDADE_CTA_FINALIZAR_SELECTOR` sem duplicado. **Re-run** da suite recomendado sobre este commit. |
| 2026-05-19 | **Checkout E2E (iteração pós falhas QA):** `comprarPageHelpers.js` — `OVERLAY_SELECTORS` sem **`.hefesto-modal__overlay`** (persistente em planilhas); filtros **`opacity`/pointer-events/visibility`** em `aguardarOverlaysInvisiveis`. **`fluxoCompra.js`** — `REGEX_INDICIOS_ETAPA_REVISAR_PEDIDO` (**checkout**, **fechar compra**, **resumo**); `textoAcumuladoElemento` com **`data-testid`**/**`data-cy`**; **`colecionarCandidatosCtaFinalPedido`** + `clicarFinalizarPedido` com prio sem depender só de `:visible`, **substring** (FINALIZ/EFETIV/…) e **`cy.get('body').contains(regex)`**. **`pedidosFiltros.js`** — `validarHubPedidos()` aceita cópias alternativas do hub (tipo de material / explorar). |
| 2026-05-18 | **Finalize pedido / loading texto / planilha 2 linhas:** `fluxoCompra.js` — `REGEX_LOADING_TEXTUAL` mais estrito (frases inteiras em vez de **`carregando…` no fim do `body`**); **`validarPedidoEnviado`** combina URLs de finalize/sucesso mais específicas com mensagens no **`innerText`** e um **`cy.contains` com retry** curto; **`finalizarPedidoNoCarrinho`** dispara segunda tentativa condicional ao CTA quando URL/texto não evoluíram logo após o 1º clique. **`planilhaUploadComplementares.cy.js`** — cenário SKU duplicado com retry e **`this.skip`** se **1×** na grade. **Re-run local (Electron/QA):** negativos de planilha ok; regressão pode falhar com **campo de emissor indisponível** — ver `cypress.env.json` / estabilidade do QA. |
| 2026-05-18 | **Emissor (landing) + SKU planilha (outerHTML):** `comprarPageEmissorActions.js` — `campoEmissorDisponivel` via **`cy.get('body')`** + **`ownerDocument.defaultView`** (valor boolean estável ao encadear `then`); heurísticas de landing/rota mantidas. **`comprarPageMetods.js`:** **`skipEmissorRecover`** em **`adicionarPrimeiroProdutoDaListaAoCarrinho`** e **`adicionarPrimeiroProdutoAoCarrinhoNaPlanilha`** (fallback catálogo após planilha força **`skip`**). **`compraPorVitrine.cy.js`** e **`planilhaUploadComplementares.cy.js`** passam **`skipEmissorRecover: true`**. **`aguardarSkuEmTextoOuHtmlPlanilha`:** conta **`tbody tr`** ou **`role=grid`/`row`** quando **`minLinhasTbody`** em `/spreadsheet`. |
| 2026-05-18 | **`allure-cypress` + checkout QA:** `cypress/support/e2e.js` — **`JSON.stringify`** com fallback **`{"__allure_truncated":true}`** em **`RangeError`**; **`Cypress.dom.stringify`** trunca (~120k) e captura **`Invalid string length`** antes de **`import 'allure-cypress'`**. **`fluxoCompra.js`** — **`validarPedidoEnviado`** aceita **`shopping-cart/configure-cart`** + texto **sucesso/protocolo/nº**; **`cy.contains`** alinhado. **`planilhaUploadComplementares.cy.js`** — steps **`cy.log`**; **`it`** 2 linhas com **`skip`** sem evidência. **`compraPorVitrine.cy.js`** — **`cy.log`** nos marcos. |
| 2026-05-18 | **Checkout revisão vs carrinho:** `fluxoCompra.js` — **`REGEX_INDICIOS_ETAPA_REVISAR_PEDIDO`** sem `\bcheckout\b` isolado (evita tratar **`/shopping-cart`** como etapa **Revisar pedido**); **`bodyTemCtaAvancarParaRevisaoVisivel`** + guarda em **`avancarCarrinhoAteEtapaRevisarPedido`** para não “pular” datas/avancaços quando o CTA **Avançar para revisão** ainda está na tela (cenário **`compraPorVitrine`** travado em **Configurar carrinho**). |
| 2026-05-21 | **Execução `npm run cy:run:fast` (QA, Electron):** **55** `it` / **~54m31s** — **27** passing, **24** failing, **4** pending (`exit_code` **24**); **21/40** specs com falha (compras/carrinho/checkout concentrados; `compraPorVitrine` em `irParaCarrinhoViaHeader`). Documentação de inventário e evidência: `RELATORIO-AUTOMACAO-E2E-TEXTO-COMPLETO.md` §1/§6, `GUIA-COBERTURA-AUTOMACAO-E2E.md`, `CHECKLIST-PRE-DEPLOY-COBERTURA-AUTOMACAO-E2E.md`, `matriz-cenarios-automacao.md`, `relatorio-gerencial-automacao-e2e.html`. |
| 2026-05-21 | **`docs/ALLURE.md`:** secção **Cypress, `allure-cypress` e DOM muito grande** (patches em `cypress/support/e2e.js`). **`planilhaUploadComplementares.cy.js`:** pós-upload com overlays + texto estável; **`planilha_2_linhas_strict`** (env) alterna assert via `aguardarSkuEmTextoOuHtmlPlanilha` vs. heurística + `skip`. `cypress.env.example.json` + `README.md` documentam a chave. |
| 2026-05-21 | **Sincronização global docs + inventário:** auditoria **55** × `it(` + **40** specs no código (**2026-05-21**); `docs/bdd/README.md` mapeamento integral spec↔`AUT`; `RELATORIO-AUTOMACAO-E2E-TEXTO-COMPLETO.md` §1 método de contagem; `GUIA-COBERTURA-AUTOMACAO-E2E.md` §4 ↔ Checklist evidência QA; harmonização AUT-012 / rodadas históricas (`CHECKLIST`, `relatorio-gerencial-automacao-e2e.html`); `matriz-cenarios-automacao.md` quadro auditoria; `ARQUITETURA.md` inventário rápido. |
| 2026-05-21 | **README onboarding:** guia rápido reescrito em `gerdau_mais_automation/README.md` (Node, `npm install`, `cypress.env.json`, `npx cypress verify`, comandos dentro da pasta vs raiz, tabela de scripts/troubleshooting, link `planilha_2_linhas_strict`). **`README.md` na raiz do monorepo** com os mesmos passos e atalhos `npm run cy:*`; correção typo “automação”. Troubleshooting CI apontando para `ALTERACOES.md` guia de cobertura. |
| 2026-05-18 | **Git (`main`):** commit **`338b524`** agrupa trabalho já descrito neste arquivo (specs/fixtures/scripts, helpers Cypress, READMEs, RELATORIO/GUIA/CHECKLIST/BDD/HTML/ALLURE). **`cypress.env.json` não foi versionado.** Publicar alterações remotas quando quiser com `git push origin main`. |
